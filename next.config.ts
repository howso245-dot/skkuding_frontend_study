import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'export',
  basePath: '/skkuding_frontend_study',
  assetPrefix: '/skkuding_frontend_study/'
};

export default nextConfig;
